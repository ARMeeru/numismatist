# Copy to .env in this directory — the Prisma CLI only reads .env from the
# package containing schema.prisma, not the repo root. See AGENTS.md "Local database".
DATABASE_URL="postgresql://numismatist:numismatist_dev@localhost:5432/numismatist_dev"
