# contracts

Single source of truth for shared types and schemas consumed by `web` and `api`: visibility enum, coin image types, the year/date model (§36.2), validation constraints. _Placeholder — stack TBD._
